module eisen.user  -- this is optional

(*                                                   *)
(*  The main function is called by the host program  *)
(*  in order to apply any overrides and extensions.  *)
(*                                                   *)

fun main = do setq greeting "Howdy, %s!\n";  -- override the greeting
              setq subject "seven seas"      -- override the receiver
           end
